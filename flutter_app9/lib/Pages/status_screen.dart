import 'package:flutter/material.dart';

class StatusScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(
     child: new Text("THIS IS STATUS SCRREN",style: new TextStyle(fontSize: 20) ),
      //child: new Icon(Icons.cloud_circle,size: 20),
    );
  }
}