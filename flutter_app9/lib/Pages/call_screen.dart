import 'package:flutter/material.dart';

class CallScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(
      //child: new Text("THIS IS CALL SCRREN",style: new TextStyle(fontSize: 20) ),
      child: new Icon(Icons.call,size: 100),
    );
  }
}