import 'package:flutter/material.dart';

class Third extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return new Container(
      child: Center(
          child: new Icon(Icons.local_pizza, size : 150, color: Colors.tealAccent)
      ),
    );
  }
}